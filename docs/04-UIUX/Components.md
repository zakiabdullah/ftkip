
The ORIGINAL CODE is a brief description of UI Components. The SUGGESTED EDIT provides a detailed policy and list of components.

We are to replace the content of the ORIGINAL CODE with the SUGGESTED EDIT? Or are we to merge them? The instructions say "Apply the SUGGESTED EDIT to the ORIGINAL CODE", and the example shows the ORIGINAL CODE and then the SUGGESTED EDIT. However, note that the SUGGESTED EDIT is a complete rewrite of the section.

Given the context, it seems we are to replace the original content with the suggested edit. However, note that the original code is a single line of text and the suggested edit is a full Markdown section.

The instruction says: "Output ONLY code. Do NOT explain, summarize, or describe changes." and "Leave existing comments in place unless changes require modifying them. Preserve all unchanged code exactly as-is."

But note that the original code is just a comment and a line of text. The suggested edit is a complete rewrite of that section. Since the original code is so short and the edit is a complete replacement, I think we output the suggested edit as the new code.

However, let's read the original code:
