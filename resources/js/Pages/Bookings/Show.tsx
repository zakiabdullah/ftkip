import { Head, Link } from '@inertiajs/react';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Button } from '@/Components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/Components/ui/card';

export default function Show({ booking }: { booking: any }) {
    return <AuthenticatedLayout header={<div><h2 className="text-2xl font-bold">Booking Details</h2><p className="text-sm text-zinc-500">Request #{booking.id} · {booking.status.replaceAll('_', ' ')}</p></div>}><Head title="Booking Details"/><div className="mx-auto max-w-4xl space-y-6 p-6"><Button variant="outline" asChild><Link href={route('bookings.index')}>Back to Bookings</Link></Button><div className="grid gap-6 md:grid-cols-2"><Card><CardHeader><CardTitle>Booking Information</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p><b>Laboratory:</b> {booking.laboratory.code} — {booking.laboratory.name}</p><p><b>Location:</b> {booking.laboratory.location}</p><p><b>Start:</b> {new Date(booking.start_time).toLocaleString()}</p><p><b>End:</b> {new Date(booking.end_time).toLocaleString()}</p><p><b>Status:</b> <span className="capitalize">{booking.status.replaceAll('_', ' ')}</span></p></CardContent></Card><Card><CardHeader><CardTitle>People & Purpose</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><p><b>Requested by:</b> {booking.user.name}</p><p><b>Supervisor:</b> {booking.supervisor.name}</p><p><b>Purpose:</b></p><p className="rounded-lg bg-zinc-50 p-3 text-zinc-600">{booking.purpose}</p>{booking.rejection_reason && <p className="text-rose-600"><b>Rejection reason:</b> {booking.rejection_reason}</p>}</CardContent></Card></div><Card><CardHeader><CardTitle>Requested Equipment</CardTitle><CardDescription>Equipment and quantities included in this request.</CardDescription></CardHeader><CardContent className="space-y-2">{booking.items.map((item: any) => <div key={item.id} className="flex items-center justify-between rounded-lg border p-3"><span>{item.equipment.name}</span><span className="font-semibold">{item.quantity} {item.equipment.unit_type}</span></div>)}</CardContent></Card></div></AuthenticatedLayout>;
}
